

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=development
    volumes:
      - ./backend/src:/app/src
    restart: always

# Frontend is expected to run locally via 'npm run dev'
